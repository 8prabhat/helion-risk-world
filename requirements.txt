# Runtime dependencies for HelionRiskWorld.

pydantic>=2.7
pyyaml>=6.0
numpy>=1.26
pandas>=2.2
polars>=0.20
pyarrow>=15.0
scikit-learn>=1.4
structlog>=24.1
rich>=13.7
click>=8.1
python-dateutil>=2.9
pytz>=2024.1
torch>=2.2
requests>=2.31

# Upstox data pipeline (package name on PyPI is upstox-python-sdk)
upstox-python-sdk>=2.26

# Free macro data (USD/INR, crude)
yfinance>=0.2.40

# dev (also available via `pip install -e .[dev]`)
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
black>=24.0
mypy>=1.10
